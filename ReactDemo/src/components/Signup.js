import React from "react";
import "./sign-up.css";

const Singup = (props) => {
  return (
    <div
      // className={"className"}
      // class="signup"
      // anotherclass={"abc"}
      // data-item={{ id: 1 }}
      // tabindex={1}
      // tabIndex={2}
      {...props}
    >
      Singup
    </div>
  );
};

export default Singup;
